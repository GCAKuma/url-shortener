# Runtime dependencies only. Pinned to exact versions so a build today and a
# build next month produce the same image. Test tooling lives in
# requirements-dev.txt so it never ships to production.
fastapi==0.138.2
uvicorn[standard]==0.49.0
sqlalchemy==2.0.51
pydantic==2.13.4
